<Window x:Class="TwitchTools.SqlConnectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SQL Login" Height="270" Width="400"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Row 0 -->
            <RowDefinition Height="Auto"/>
            <!-- Row 1 -->
            <RowDefinition Height="Auto"/>
            <!-- Row 2 -->
            <RowDefinition Height="Auto"/>
            <!-- Row 3 -->
            <RowDefinition Height="Auto"/>
            <!-- Row 4 -->
            <RowDefinition Height="Auto"/>
            <!-- Row 5 (optional spacing row) -->
            <RowDefinition Height="Auto"/>
            <!-- Row 6 for Save button -->
        </Grid.RowDefinitions>

        <!-- GroupBox spanning only rows 0 to 4 now -->
        <GroupBox Header="Master DB" Grid.Row="0" Grid.RowSpan="5" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Instance:" Margin="0,0,10,5" VerticalAlignment="Center"/>
                <TextBox Name="DataSourceTextBox" Grid.Row="0" Grid.Column="1" Height="22" Margin="0,0,0,5"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Database:" Margin="0,0,10,5" VerticalAlignment="Center"/>
                <TextBox Name="InitialCatalogTextBox" Grid.Row="1" Grid.Column="1" Height="22" Margin="0,0,0,5"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Login:" Margin="0,0,10,5" VerticalAlignment="Center"/>
                <TextBox Name="UserIdTextBox" Grid.Row="2" Grid.Column="1" Height="22" Margin="0,0,0,5"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Password:" Margin="0,0,10,5" VerticalAlignment="Center"/>
                <PasswordBox Name="PasswordBox" Grid.Row="3" Grid.Column="1" Height="22" Margin="0,0,0,5"/>

                <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                    <CheckBox Name="UseIpAddressCheckBox" Content="Use IP Address" VerticalAlignment="Center" Margin="5" />
                    <Button Content="Connect" Click="SaveButton_Click" Width="80" Margin="5,5,0,5"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Save button now properly placed in Row 6 -->
        <Button Grid.Row="6" Content="Save" Click="SaveButton_Click" Width="80" HorizontalAlignment="Right" Margin="0,10,0,0"/>
    </Grid>
</Window>
